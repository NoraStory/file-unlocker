健壮性改进版本，修复几处并发与误操作风险。

## 改进

- **并发扫描防护**：连续拖入/右键多个文件时，两次扫描可能交叠，界面出现"文件名 A 配进程列表 B"的错乱。现在始终只显示最后一次请求的结果，早到的作废。
- **结束进程树的防误伤**：结束树操作现在拒绝包含本程序自身、System 进程和 explorer.exe（误杀会让桌面崩溃重建）。
- **文件夹模式明确提示**：右键文件夹查询占用时，界面显示"文件夹模式"并隐藏不适用的删除按钮。
- **文件选择器打不开时给出原因**：之前失败只在控制台记录，界面零反馈（正是 v0.1.3 修的那个问题的暴露方式）。现在会显示错误横幅。
- **注册脚本支持安装版**：双击注册脚本现在会依次查找开发产物、Program Files 和 LocalAppData 下的安装位置，不再只能识别编译产物。

## 下载

- FileUnlocker_0.1.4_x64-setup.exe — 安装版
- file-unlocker.exe — 免安装单文件
