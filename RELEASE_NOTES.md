性能版本：文件夹扫描速度提升约 50 倍。

## 性能

- **文件夹扫描提速约 50 倍**：实测 100 文件目录从约 24 秒降到 0.5 秒以内。原因是旧实现对每个文件都完整遍历一次系统句柄表（约 27 万句柄），N 个文件就是 N 次全表遍历；现在改为一次遍历解析出全部句柄路径，再与目录做前缀匹配。检测精度不变——仍是完整路径逐一比对，两个引擎（Restart Manager + 句柄扫描）都做了同样的批量化，RM 侧还合并为单会话批量注册（每批 500 个文件）。
- 大目录（上千文件）的扫描进度条照常工作。

## 下载

- FileUnlocker_0.2.0_x64-setup.exe — 安装版
- file-unlocker.exe — 免安装单文件
