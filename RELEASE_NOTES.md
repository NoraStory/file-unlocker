文件夹占用检测版本。

## 新功能

- **文件夹占用检测**：把整个文件夹拖入（或右键文件夹选择"解除文件夹占用"），程序会递归检测内部所有文件的占用情况，按进程汇总——显示"某进程占用了 N 个文件"，一键结束即可释放整个文件夹。上限 2000 个文件，无权限的子目录自动跳过。

此前文件夹模式只查询目录本身，检测结果恒为空（锁定的是内部文件的句柄，与目录路径不匹配）。

## 下载

- FileUnlocker_0.1.5_x64-setup.exe — 安装版
- file-unlocker.exe — 免安装单文件
